# Vite frontend config. Copy to .env (or .env.local) and adjust.
VITE_API_BASE=http://localhost:8000
# WebSocket base for live scan. Defaults to VITE_API_BASE with ws://.
# VITE_WS_BASE=ws://localhost:8000
